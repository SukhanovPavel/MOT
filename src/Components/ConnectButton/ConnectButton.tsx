import React from 'react';
import styles from "./ConnectButton.module.css";

export const ConnectButton = () => {
    return (
        <button className={styles.connectButton}>Подключиться к сети</button>
    );
};